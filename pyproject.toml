[project]
name = "qdii-quota-radar"
version = "0.1.0"
description = "QDII fund purchase-limit monitor"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
pattern = "test_*.py"

[tool.ruff]
target-version = "py311"
line-length = 100

